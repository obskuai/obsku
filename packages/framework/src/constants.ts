/** Milliseconds per second - for converting between ms and seconds */
export const MS_PER_SECOND = 1000;

/** Default thinking budget tokens for LLM providers that support extended thinking */
export const DEFAULT_THINKING_BUDGET_TOKENS = 4096;

/** Tokens to reserve for output in context window management */
/** Tokens to reserve for output in context window management */
export const RESERVE_OUTPUT_TOKENS = 4096;

/** Maximum length for log/error message previews (200 characters) */
export const LOG_PREVIEW_MAX_LENGTH = 200;
